class Ingredient {
String name; // Name of the ingredient (e.g., "Sugar")
int quantity; // Quantity of the ingredient (e.g., "2")
String unit; // Unit of measurement (e.g., "cups")

 // Constructor
Ingredient({required this.name, required this.quantity, required this.unit});
}